on:
  push:
    branches:
      - dev

jobs:
  newImage:
    runs-on: self-hosted
    name: Create a new image && deploy it
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v4
      # Use the output from the `hello` step
      - name: Create a new image
        run: ssh debian@pariscaretaker.fr 'cd /var/API && sudo docker build -t dev-api-image .'
      - name: Update the Traefik configuration
        run: ssh debian@pariscaretaker.fr 'cd /var/reverse-proxy && sudo docker compose up -d'